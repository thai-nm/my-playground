_format_version: "3.0"
_transform: true

services:
- name: user-service
  host: user-service
  port: 8080
  protocol: http
  routes:
  - name: user-service-route
    paths:
    - /user-service
    strip_path: true